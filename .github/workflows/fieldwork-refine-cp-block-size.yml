name: Fieldwork refine cp block size

on:
  push:
    branches:
      - fieldwork/cp-sparse-early-eof-12648
  pull_request:
    branches:
      - base/canonical-main-20260804

permissions:
  contents: write

jobs:
  verify-and-promote:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          fetch-depth: 0
          persist-credentials: true

      - uses: dtolnay/rust-toolchain@2c7215f132e9ebf062739d9130488b56d53c060c
        with:
          toolchain: stable
          components: clippy,rustfmt

      - uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32

      - name: Apply and format refinement
        run: |
          set -euo pipefail
          python3 .fieldwork/refine-cp-block-size.py
          cargo fmt --all
          test "$(git diff --name-only | sort)" = 'src/uu/cp/src/platform/linux.rs'
          cargo fmt --all -- --check
          git diff --check

      - name: Test deterministic early EOF transition
        run: cargo test -p uu_cp sparse_copy_data_truncates_destination_on_early_eof --lib

      - name: Run cp integration module
        run: cargo test --features cp --no-default-features --test tests test_cp

      - name: Run focused clippy
        run: cargo clippy --features cp --no-default-features --all-targets -- -D warnings

      - name: Promote source-only refinement
        if: github.event_name == 'push'
        run: |
          set -euo pipefail
          test "$(git diff --name-only | sort)" = 'src/uu/cp/src/platform/linux.rs'
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/uu/cp/src/platform/linux.rs
          git rm .fieldwork/refine-cp-block-size.py .github/workflows/fieldwork-refine-cp-block-size.yml
          git commit -m "cp: avoid sparse block-size conversion panic"
          git push origin HEAD:"${GITHUB_REF_NAME}"
